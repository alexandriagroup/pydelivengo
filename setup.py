from setuptools import setup

tests_require = [
    'pytest',
    'pytest-pep8',
    'pytest-cov',
]

setup(
    name='pydelivengo',
    keywords=['api', 'mydelivengo', 'python', 'webservices'],
    license='MIT',
)
